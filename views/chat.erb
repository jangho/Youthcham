<!DOCTYPE html>
<html>
<head>
  <title>청년참</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="stylesheet" href="reset.css"/>
  <link rel="stylesheet" href="main.css"/>
  <link rel="stylesheet" href="detail.css"/>

</head>
<body class="bg-gray">
<%= erb :header %>
<section class="section">
  <div class="container">
    <h1 class="section-site-title"><p><img src="http://imageshack.com/a/img903/3843/hBIbVH.png"></p>잡담해요</h1>
  </div>
</section>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="well">
      <div class="media">
        <div class="media-body">
          <% if !@user.nil? %>
              <p><a href="/add_chat">글쓰기</a></p>
          <% end %>

          <% @chats.each do |p| %>
              <a href="/chat_detail/<%= p.id %>">
                <h4 class="media-heading"><%= p.title %></h4>
              </a>

              <p><%= p.user_id %></p>

              <p><%= p.created_at.strftime("%Y-%m-%d") %></p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
<%= erb :footer %>
</body>
</html>
